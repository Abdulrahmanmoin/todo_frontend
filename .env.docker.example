# Frontend Environment Configuration - Docker Deployment
# Copy this file to .env for Docker deployment

# API URL for Docker (use service name 'backend' for internal communication)
NEXT_PUBLIC_API_URL=http://backend:8000

# App URL (optional)
# NEXT_PUBLIC_APP_URL=http://localhost:3000

# Better Auth Configuration
BETTER_AUTH_SECRET=/73daibX6p0MBoevZOwQZp+5wkv0bS25u8/Gyoc4eSU=
BETTER_AUTH_URL=http://localhost:3000

# Database URL (for Better Auth if using database adapter)
# DATABASE_URL will be provided by docker-compose.yml
